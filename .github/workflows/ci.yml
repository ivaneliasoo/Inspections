name: Docker Image CI

on:
  push:
    tags:
      - test-*
      - prod-*
jobs:

  build-backend:

    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: [ubuntu-latest]
        
    steps:
      - uses: actions/checkout@v2
      - name: Build the API Docker image
        run: docker build src/Backend --file Dockerfile --tag scb-backend:${GITHUB_REF/refs\/tags\//}
