<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ico="clr-namespace:ReportsApp.Styles"
             x:Class="ReportsApp.Control.CheckItem">
    <ContentView.Content>
        <Frame CornerRadius="5" HasShadow="True">
            <Frame.GestureRecognizers>
                <TapGestureRecognizer x:Name="tapGesture" NumberOfTapsRequired="1"  />
            </Frame.GestureRecognizers>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>
                <StackLayout Grid.Column="0" Grid.Row="0">
                    <Label x:Name="checkText" FontSize="Small" />
                </StackLayout>
                <StackLayout Grid.Column="1" >
                    <Label x:Name="lblIcon" VerticalOptions="CenterAndExpand" HorizontalOptions="EndAndExpand" 
                       FontSize="Large" FontFamily="FontAwesome" />
                    <Label x:Name="checkValue" FontSize="Micro" VerticalOptions="CenterAndExpand" HorizontalOptions="EndAndExpand"  />
                </StackLayout>
                
            </Grid>
        </Frame>
    </ContentView.Content>
</ContentView>